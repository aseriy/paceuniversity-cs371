#cloud-config
autoinstall:
  version: 1

  identity:
    hostname: temporary
    username: admin
    password: "!"

  ssh:
    install-server: true
    allow-pw: false
    authorized-keys:
      - ssh-ed25519 YOUR_PUBLIC_KEY

  storage:
    layout:
      name: direct

  packages:
    - git
    - curl

  late-commands:
    - |
      iface=$(ip route show default | awk 'NR==1 {print $5}')
      mac=$(tr -d ':' < "/sys/class/net/$iface/address")
      hostname="cs371-${mac#??????}"
      printf '%s\n' "$hostname" > /target/etc/hostname
      sed -i "s/temporary/$hostname/g" /target/etc/hosts

